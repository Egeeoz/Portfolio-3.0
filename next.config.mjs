/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Portfolio-3.0',
  assetPrefix: '/Portfolio-3.0/',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
